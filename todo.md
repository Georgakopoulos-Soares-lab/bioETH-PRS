# TODO

## Notes

remove vendor repo, update npm deps
fhevm coprocessor

---

generate mock data for tests (e.g. encrypted SNP arrays, model weights) with chatgpt (try without DP noise first, then add noise in a second pass)

200, 1000 snips - 200, 1000 weights 1:1 mapping

# N muls in one transaction exceeds gas limit → chunked approach

transaction approach

concurrency / parallel

float to integer arithmetic scaling factor (e.g. 10^8) — need to balance precision vs risk of overflow

vendor does that with weights, returned result also divided by same
